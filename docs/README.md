# Documentation Index

## User Docs

- `docs/user/quickstart.md`: setup and common command flows.
- `docs/user/configuration.md`: `config.yaml`, `.env`, paths, model, ensemble, and Safe Agent settings.
- `docs/user/operations.md`: daily DB refresh, validation, Phase2 model operations, and generated outputs.

## Codex Docs

- `docs/codex/context.md`: compact architecture and invariants for AI coding agents.
- `docs/codex/commands.md`: verification commands and when to run them.

The root `README.md` is the public overview. These docs hold operational details so future edits do not need to reread one long file.
