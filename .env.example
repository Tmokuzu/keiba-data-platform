POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=keiba
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password

# Optional: keep empty unless your PostgreSQL server requires SSL.
# POSTGRES_SSLMODE=disable
